import { ChangeDatePipe } from './change-date.pipe';

describe('ChangeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
